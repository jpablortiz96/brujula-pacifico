# Models

Este directorio es un mapa conceptual, no una copia del codigo. BRUJULA no versiona modelos `.pkl`, embeddings ni notebooks falsos.

- `scoring/`: score de olvido y RPC.
- `agentic-ai/`: agente Claude con herramientas.
- `simulation/`: simulador de escenarios.

